#services:
#  postgres:
#    image: postgres:16-alpine
#    container_name: gestor_cred_pg
#    restart: unless-stopped
#    environment:
#      POSTGRES_USER: creds
#      POSTGRES_PASSWORD: creds_secret
#      POSTGRES_DB: creds_db
#    ports:
#      - '5432:5432'
#    volumes:
#      - gestor_cred_pgdata:/var/lib/postgresql/data
#    healthcheck:
#      test: ['CMD-SHELL', 'pg_isready -U creds -d creds_db']
#      interval: 5s
#      timeout: 5s
#      retries: 5

  # Backend opcional: descomenta para ejecutar la API dentro de Docker
  # api:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - '3333:3333'
  #   environment:
  #     HOST: 0.0.0.0
  #     PORT: 3333
  #   env_file:
  #     - ./backend/.env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  gestor_cred_pgdata:
